#!/bin/sh
# Resets the $DISPLAY variable in order to relink the Debian (WSL)
# with the Windows screen.
# One use scenario of this funciton is the debug of Cypress in case the tool
# start correctly, but the GUI does not appear on screen.

export DISPLAY=:0
